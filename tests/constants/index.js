module.exports = {
  errorDir: `${__dirname}/../project/error/`,
  secureDir: `${__dirname}/../project/secure/`,
  config: {
    label: false,
    backup: false,
    frozen: false,
    verbose: false,
    pattern: '--caret'
  }
};
